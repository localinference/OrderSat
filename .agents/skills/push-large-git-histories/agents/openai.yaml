interface:
  display_name: 'Push Large Histories'
  short_description: 'Push large Git histories and assets safely'
  default_prompt: 'Use $push-large-git-histories to push a large local Git branch to a remote safely and verify both refs match.'
