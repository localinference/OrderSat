/**
 * 1. lists ./src/02_training_samples/outputs/${langauge} dir
 * 2. lists ./src/02_training_samples/inputs/${langauge} dir
 * 3. diffs inputs without outputs to temp-unannotated at cwd root
 * 4. annotators start annotating according to annotation skill (manually) not relevant for script and delete annotated from temp un annotated when finnished
 */
