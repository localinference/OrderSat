[project]
name = "OrderSaT"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy",
    "onnx",
    "onnx-ir",
    "onnxruntime",
    "onnxscript",
    "sentencepiece",
    "torch",
]

[tool.uv]
package = false
